
### 1. 有向图

- 对于一条边，从一个点到另一个点是单向的

- 如`1 -> 3`，是单向的

- 若再给 3 -> 1，就是反向的

### 2. 带权图

- 即边还带有一定权重，比如3、6等整数

### 3. 无向图

- 可以视为一种特殊的有向图

	- 因为其可以视作存在边的两个点之间有两条有向边，两条边互为反向
	
	- 比如`[3, 5]`存在一条边，则可视为有向图中的`[3, 5]`和`[5, 3]`两条边